<div class="part-results">
    <section class="person-list">

        <h2 class="text-xl">Resultaten</h2>
        {% if persons.size  > 0 %}
            {% for person in persons %}
            {% render "partials/post-types/person-loop.liquid"  person: person%}
            {% endfor %}
        {% else %}
            <p>Je hebt een slechte smaak, niemand vind  {{ fav }} lekker</p>
        {% endif %}
    </section>
</div>